# Vue 3 + TypeScript + Vite Template

> Quick Start And Run

```shell
git clone https://github.com/Amayakite/web-template.git
cd web-template
npm install -g pnpm
pnpm i
npx vite
# or 
pnpm dev
```

> Build

```shell
pnpm build
# or 
npx vite build
```